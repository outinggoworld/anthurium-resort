// Dates that are already booked, in "YYYY-MM-DD" format.
//
// IMPORTANT: left empty on purpose. Per the content rule (never invent
// data), this file should be wired up to real availability data — either
// manually updated by the resort team, or pulled from a booking system —
// before this goes live. Until then every date shows as "Available".
export const bookedDates: string[] = [];
